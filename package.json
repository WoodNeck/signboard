{
  "name": "signboard",
  "version": "0.0.1",
  "description": "",
  "main": "dist/signboard.js",
  "module": "dist/signboard.esm.js",
  "types": "declaration/index.d.ts",
  "scripts": {
    "dev": "concurrently \"rollup -w --config ./rollup.config.dev.js\" \"http-serve\"",
    "build": "rm -rf ./dist && rollup -c && npm run declaration",
    "declaration": "rm -rf ./declaration && tsc -p tsconfig.declaration.json",
    "demo:build": "npm run build && cpx 'dist/**/*' demo/release/latest/dist --clean",
    "demo:prebuild-version": "cpx 'dist/**/*' demo/release/$npm_package_version/dist --clean && cpx 'docs/**/*' demo/release/$npm_package_version/docs --clean",
    "demo:prebuild-latest": "cpx 'dist/**/*' demo/release/latest/dist --clean && cpx 'docs/**/*' demo/release/latest/docs --clean",
    "demo:deploy": "npm run build && npm run doc && npm run demo:prebuild-version && npm run demo:prebuild-latest && gh-pages -d demo/",
    "doc": "rm -rf ./docs && jsdoc -c jsdoc.json"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/WoodNeck/signboard.js.git"
  },
  "author": "WoodNeck",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/WoodNeck/signboard.js/issues"
  },
  "homepage": "https://github.com/WoodNeck/signboard.js#readme",
  "devDependencies": {
    "@daybrush/jsdoc": "^0.3.7",
    "@egjs/build-helper": "0.0.5",
    "bulma": "^0.9.2",
    "concurrently": "^6.2.0",
    "cpx": "^1.5.0",
    "gh-pages": "2.0.1",
    "http-serve": "^1.0.1",
    "rollup": "^1.10.1",
    "rollup-plugin-glslify": "^1.2.0",
    "rollup-plugin-livereload": "^2.0.0",
    "tslint-consistent-codestyle": "^1.15.1",
    "typescript": "^4.3.3"
  },
  "dependencies": {}
}
