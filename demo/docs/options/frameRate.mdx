import Columns from "@site/src/components/Columns";
import ColumnItem from "@site/src/components/ColumnItem";
import BlackSignBoard from "@site/src/components/BlackSignBoard";
import useBaseUrl from "@docusaurus/useBaseUrl";

A number of rendering frames per second for video / text.

<Columns>
  <ColumnItem is={6}>

  ```js
  // Default
  frameRate: 60
  ```

  </ColumnItem>
  <ColumnItem is={6}>
    <BlackSignBoard className="options-canvas" src={useBaseUrl("/video/Astronaut.mp4")} contentType="video" />
  </ColumnItem>
</Columns>
<Columns>
  <ColumnItem is={6}>

  ```js
  frameRate: 1
  ```

  </ColumnItem>
  <ColumnItem is={6}>
    <BlackSignBoard className="options-canvas" src={useBaseUrl("/video/Astronaut.mp4")} contentType="video" frameRate={1} />
  </ColumnItem>
</Columns>
